name: AetherGrid AIO (AetherShard v16)

on:
  workflow_dispatch:
    inputs:
      force_reinstall:
        description: 'Wipe shards and reinstall OS (true/false)'
        required: false
        default: 'false'

jobs:
  boot-vm:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    env:
      NB_SK:  ${{ secrets.NB_SK }}
      DRIME:  ${{ secrets.DRIME }}
      DRIME1: ${{ secrets.DRIME1 }}
      DRIME2: ${{ secrets.DRIME2 }}
      DRIME3: ${{ secrets.DRIME3 }}

    steps:
      - name: Install Minimal Stack
        run: |
          set -e
          sudo apt-get update -y
          sudo apt-get remove -y rclone || true
          sudo apt-get install -y --no-install-recommends \
            qemu-system-x86 qemu-utils qemu-system-common fuse3 wget curl ca-certificates python3
          curl https://rclone.org/install.sh | sudo bash
          curl -fsSL https://pkgs.netbird.io/install.sh | sh
          echo "[LOG] WHAT: INSTALL_DONE | WHEN: $(date +%s%N)"

      - name: NetBird Mesh Up
        run: |
          set -e
          sudo netbird up --setup-key "$NB_SK" --log-level error &
          sleep 15
          echo "[LOG] WHAT: NETBIRD_UP | WHEN: $(date +%s%N)"

      - name: Build Drime Config
        run: |
          set -e
          CONF=/tmp/rclone.conf; : > $CONF
          printf '[drime0]\ntype = drime\naccess_token = %s\n' "$DRIME" >> $CONF
          N=1; i=1
          for V in DRIME1 DRIME2 DRIME3; do
            VAL="${!V}"
            if [ -n "$VAL" ]; then
              printf '[drime%d]\ntype = drime\naccess_token = %s\n' "$i" "$VAL" >> $CONF
              N=$((N+1)); i=$((i+1))
            fi
          done
          if [ "$N" -gt 1 ]; then
            UP=""
            for k in $(seq 0 $((N-1))); do UP="$UP drime$k:"; done
            printf '[union]\ntype = union\nupstreams =%s\ncreate_policy = epmfs\naction_policy = epall\nsearch_policy = ff\n' "$UP" >> $CONF
            REMOTE=union
          else
            REMOTE=drime0
          fi
          chmod 600 $CONF
          echo "REMOTE=$REMOTE" >> $GITHUB_ENV
          echo "ACCOUNTS=$N"    >> $GITHUB_ENV
          echo "[LOG] WHAT: CONFIG_BUILT | ACCOUNTS: $N | REMOTE: $REMOTE | WHEN: $(date +%s%N)"

      - name: Self-Heal + Mode + Virtual Size
        run: |
          set -e
          if [ "${{ github.event.inputs.force_reinstall }}" = "true" ]; then
            rclone purge --config /tmp/rclone.conf "${REMOTE}:vm" || true
            echo "[LOG] WHAT: SELF_HEAL | purged vm shards | WHEN: $(date +%s%N)"
          fi
          EXIST=$(rclone lsf --config /tmp/rclone.conf "${REMOTE}:vm" 2>/dev/null | head -1 || true)
          if [ -n "$EXIST" ]; then
            MODE=BOOT
            if [ "$ACCOUNTS" -eq 1 ]; then VSIZE=18; else VSIZE=$((18*ACCOUNTS-4)); fi
          else
            MODE=FIRST
            if [ "$ACCOUNTS" -eq 1 ]; then VSIZE=16; else VSIZE=$((18*ACCOUNTS-4)); fi
          fi
          echo "MODE=$MODE"   >> $GITHUB_ENV
          echo "VSIZE=$VSIZE" >> $GITHUB_ENV
          echo "[LOG] WHAT: MODE_DETECT | MODE: $MODE | VSIZE: ${VSIZE}G | WHEN: $(date +%s%N)"

      - name: Stream ISO To Drime (rcat pipe, zero local)
        if: env.MODE == 'FIRST'
        run: |
          set -e
          HAS=$(rclone lsf --config /tmp/rclone.conf "${REMOTE}:" 2>/dev/null | grep -c '^ubuntu.iso$' || true)
          if [ "${HAS:-0}" = "0" ]; then
            echo "[LOG] WHAT: ISO_STREAM_TO_DRIME | VIA: rcat pipe | WHEN: $(date +%s%N)"
            wget -qO- "https://releases.ubuntu.com/resolute/ubuntu-26.04-live-server-amd64.iso" \
              | rclone --config /tmp/rclone.conf rcat "${REMOTE}:ubuntu.iso"
          else
            echo "[LOG] WHAT: ISO_REUSE | FROM: Drime | WHEN: $(date +%s%N)"
          fi

      - name: Mount Drime Read-Only Path (cache-off)
        run: |
          set -e
          sudo mkdir -p /mnt/drime
          sudo sed -i 's/#user_allow_other/user_allow_other/' /etc/fuse.conf || true
          sudo rclone mount "${REMOTE}:" /mnt/drime \
            --config /tmp/rclone.conf --vfs-cache-mode off \
            --allow-other --log-level INFO --daemon
          sleep 8
          mountpoint -q /mnt/drime
          echo "[LOG] WHAT: DRIME_MOUNT_OK | READ_PATH: cache-off | WHEN: $(date +%s%N)"

      - name: Write AetherShard Daemon
        run: |
          cat > /tmp/aethershard.py <<'PYEOF'
          import os,socket,struct,threading,subprocess,time,signal
          from concurrent.futures import ThreadPoolExecutor
          CONF="/tmp/rclone.conf"; REMOTE=os.environ["REMOTE"]; VSIZE=int(os.environ["VSIZE"])*1024**3
          SHARD=4*1024*1024; PORT=10809; DIRTY_MAX=256*1024*1024
          RC=["rclone","--config",CONF]
          lock=threading.Lock(); cond=threading.Condition(lock)
          dirty={}; touched={}; uploading={}; fetching=set(); stats={"put":0,"get":0}
          def name(i): return f"vm/shard_{i:06d}"
          def put(i,data):
              p=subprocess.run(RC+["rcat",f"{REMOTE}:{name(i)}"],input=bytes(data),capture_output=True)
              return p.returncode==0
          def get(i):
              p=subprocess.run(RC+["cat",f"{REMOTE}:{name(i)}"],capture_output=True)
              if p.returncode==0 and p.stdout:
                  stats["get"]+=1; return bytearray(p.stdout)
              return bytearray(SHARD)
          def uploader():
              pool=ThreadPoolExecutor(4); futs={}
              while True:
                  with lock:
                      now=time.time()
                      for i in [i for i in dirty if now-touched[i]>2.0 and i not in futs]:
                          uploading[i]=dirty.pop(i); futs[i]=pool.submit(put,i,uploading[i])
                      for i in [i for i,f in futs.items() if f.done()]:
                          f=futs.pop(i); ok=f.result()
                          if ok: stats["put"]+=1; uploading.pop(i,None)
                          else: dirty[i]=uploading.pop(i); touched[i]=time.time()
                      cond.notify_all()
                  time.sleep(0.5)
          def drain():
              while True:
                  with lock:
                      if not dirty and not uploading: return
                  time.sleep(0.3)
          def shard_buf(i):
              with lock:
                  if i in dirty: return dirty[i]
                  if i in uploading: return uploading[i]
                  while i in fetching: cond.wait(0.2)
                  fetching.add(i)
              b=get(i)
              with lock:
                  fetching.discard(i)
                  if i not in dirty: dirty[i]=b; touched[i]=time.time()
                  return dirty[i]
          def write_at(off,data):
              pos=0
              while pos<len(data):
                  i=off//SHARD; s=off-i*SHARD; n=min(SHARD-s,len(data)-pos)
                  b=shard_buf(i)
                  with lock:
                      b[s:s+n]=data[pos:pos+n]; touched[i]=time.time()
                      while len(dirty)*SHARD>DIRTY_MAX: cond.wait(0.2)
                  off+=n; pos+=n
          def read_at(off,length):
              out=bytearray(); pos=0
              while pos<length:
                  i=off//SHARD; s=off-i*SHARD; n=min(SHARD-s,length-pos)
                  out+=shard_buf(i)[s:s+n]; off+=n; pos+=n
              return bytes(out)
          def handle(conn):
              f=conn.makefile("rwb")
              f.write(b"NBDMAGIC"+struct.pack(">Q",0x49484156454f5054)+struct.pack(">H",0)); f.flush()
              opt=struct.unpack(">I",f.read(4))[0]
              f.write(struct.pack(">Q",VSIZE)+struct.pack(">I",0x0005)+b"\x00"*124); f.flush()
              while True:
                  hdr=f.read(28)
                  if len(hdr)<28: return
                  magic,typ,handle,off,ln=struct.unpack(">IIQQI",hdr)
                  if typ==0:
                      f.write(struct.pack(">IIQ",0x67446698,0,handle)+read_at(off,ln)); f.flush()
                  elif typ==1:
                      d=f.read(ln); write_at(off,d)
                      f.write(struct.pack(">IIQ",0x67446698,0,handle)); f.flush()
                  elif typ==3:
                      drain(); f.write(struct.pack(">IIQ",0x67446698,0,handle)); f.flush()
                  elif typ==2:
                      drain(); f.write(struct.pack(">IIQ",0x67446698,0,handle)); f.flush(); return
                  else:
                      f.write(struct.pack(">IIQ",0x67446698,0,handle)); f.flush()
          def on_term(sig,frm):
              threading.Thread(target=lambda:(drain(),os._exit(0))).start()
          signal.signal(signal.SIGTERM,on_term); signal.signal(signal.SIGINT,on_term)
          threading.Thread(target=uploader,daemon=True).start()
          srv=socket.socket(); srv.setsockopt(socket.SOL_SOCKET,socket.SO_REUSEADDR,1)
          srv.bind(("127.0.0.1",PORT)); srv.listen(1)
          print(f"[LOG] WHAT: AETHERSHARD_READY | PORT: {PORT} | VSIZE: {VSIZE} | WHEN: {time.time_ns()}",flush=True)
          while True:
              conn,_=srv.accept(); threading.Thread(target=handle,args=(conn,),daemon=True).start()
          PYEOF
          python3 -m py_compile /tmp/aethershard.py
          echo "[LOG] WHAT: DAEMON_WRITTEN_AND_COMPILED | WHEN: $(date +%s%N)"

      - name: Launch Daemon + Assert Alive
        run: |
          set -e
          REMOTE=$REMOTE VSIZE=$VSIZE nohup python3 /tmp/aethershard.py > /tmp/aethershard.log 2>&1 &
          sleep 6
          cat /tmp/aethershard.log
          grep -q AETHERSHARD_READY /tmp/aethershard.log
          (exec 3<>/dev/tcp/127.0.0.1/10809) 2>/dev/null || { echo "[LOG] WHAT: NBD_PORT_DEAD | WHEN: $(date +%s%N)"; exit 1; }
          echo "[LOG] WHAT: DAEMON_ALIVE | NBD: 10809 | WHEN: $(date +%s%N)"

      - name: Boot VM (Disk = Drime Shards via NBD, VNC 5900)
        run: |
          if [ -e /dev/kvm ]; then ACC="-accel kvm -cpu host"; else ACC="-accel tcg -cpu max"; fi
          ARGS="-m 12288 -smp 4 $ACC -vnc 0.0.0.0:0 -display none"
          ARGS="$ARGS -drive file=nbd:127.0.0.1:10809,if=virtio,format=raw,cache=none"
          if [ "$MODE" = "FIRST" ]; then ARGS="$ARGS -boot order=dc -cdrom /mnt/drime/ubuntu.iso"; else ARGS="$ARGS -boot order=c"; fi
          echo "[LOG] WHAT: VM_START | MODE: $MODE | DISK: NBD->Drime | VNC: 0.0.0.0:5900 | WHEN: $(date +%s%N)"
          ( while true; do sleep 300; \
            echo "[LOG] WHAT: HEARTBEAT | MODE: $MODE | WHEN: $(date +%s%N)"; \
            free -h | head -2 || true; \
            echo "[LOG] WHAT: SHARD_COUNT | N: $(rclone lsf --config /tmp/rclone.conf "${REMOTE}:vm" 2>/dev/null | wc -l) | WHEN: $(date +%s%N)"; \
          done ) &
          HB=$!
          sudo qemu-system-x86_64 $ARGS &
          QP=$!
          EXIT=0; wait $QP || EXIT=$?
          kill $HB 2>/dev/null || true
          echo "VMEXIT=$EXIT" >> $GITHUB_ENV
          echo "[LOG] WHAT: VM_EXIT | CODE: $EXIT | WHEN: $(date +%s%N)"
          exit 0

      - name: Drain + Guarded Cleanup (ALWAYS)
        if: always()
        run: |
          echo "[LOG] WHAT: DRAIN_START | WHEN: $(date +%s%N)"
          sudo pkill -TERM -f qemu-system-x86_64 || true
          sleep 3
          pkill -TERM -f aethershard.py || true
          sleep 15
          tail -5 /tmp/aethershard.log || true
          echo "[LOG] WHAT: FINAL_SHARDS | N: $(rclone lsf --config /tmp/rclone.conf "${REMOTE}:vm" 2>/dev/null | wc -l) | WHEN: $(date +%s%N)"
          if [ "$MODE" = "FIRST" ] && [ "$VMEXIT" = "0" ] && [ "$ACCOUNTS" -eq 1 ]; then
            rclone deletefile --config /tmp/rclone.conf "${REMOTE}:ubuntu.iso" || true
            echo "[LOG] WHAT: ISO_CLEANUP | quota freed for 18G | WHEN: $(date +%s%N)"
          fi
          sudo umount /mnt/drime || sudo fusermount3 -u /mnt/drime || true
          echo "[LOG] WHAT: DRAIN_DONE | WHEN: $(date +%s%N)"
